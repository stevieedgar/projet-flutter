name: namer_app
description: A new Flutter project.

publish_to: 'none'

version: 0.0.1+1

environment:
  sdk: '>=2.19.4 <3.8.0'

dependencies:
  flutter:
    sdk: flutter

  english_words: ^4.0.0
  provider: ^6.1.2
  async: 2.12.0  # Version compatible avec flutter_test
  material_color_utilities: 0.11.1
  vector_math: 2.1.4

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^5.0.0
  lints: ^5.1.1
  fake_async: 1.3.2
  leak_tracker: 10.0.8
  vm_service: 14.3.1

flutter:
  uses-material-design: true
